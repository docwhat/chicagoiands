<!DOCTYPE html>
<html>
<head>
  <title>ChicagoIANDS</title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= params[:controller] %> <%= "#{params[:controller]}_#{params[:action]}" %>">

<header id="header"><%= link_to "Chicago IANDS", :root %></header>

<% if current_user %>
  <section id="login-bar">
    Logged in as <%= current_user.logon %>
    <%= link_to "Log out", logout_path %>
  </section>
<% end %>

<% flash.each do |name, msg| %>  
    <%= content_tag :div, msg, :id => "flash#{name}" %>  
<% end %>

<div id="content">
<%= yield %>
</div>

<footer id="footer">
  Copyright 2000-<%= Date.today.year %> Chicago IANDS, unless otherwise noted. You are encouraged to share the information at this web site with other people, as long as you do not charge for it.
</footer>
</body>
</html>
